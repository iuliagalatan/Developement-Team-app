#pragma once

#include <vector>
#include <string>
#include <sstream>
using namespace  std;

std::string trim(const std::string& s);

vector<string> split(const string& str, char delimiter);
